<!DOCTYPE html>

<html>
  <head>
    <meta charset="utf-8">
    <title>IMDAZ - Gerenciamento de Estudantes | Listagem de alunos</title>
    <link href="/css/main.css" rel="stylesheet">
  </head>

  <body>
    <main>
      <h1>{{ title }}</h1>
      <p>{{ description }}</p>
      
      <section>
        {{#if students}}
          <ul>
            {{#each students}}
              <li>{{this}}</li>
            {{/each}}
          </ul>
        {{else}}
          <p>Não há registros de estudantes para serem exibidos.</p>
        {{/if}}
      </section>
    </main>
  </body>
</html>