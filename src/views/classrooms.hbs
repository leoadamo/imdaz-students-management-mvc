<!DOCTYPE html>

<html>
  <head>
    <meta charset="utf-8">
    <title>IMDAZ - Gerenciamento de Estudantes | Turmas</title>
    <link href="/css/main.css" rel="stylesheet">
  </head>

  <body>
    <main>
      <h1>{{ title }}</h1>
      <p>{{ description }}</p>

      {{#if response}}
        <ul>
          {{#each response}}
            {{#each this}}
              <li>{{this}}</li>
            {{/each}}
          {{/each}}
        </ul>
      {{else}}
        <p>Não há registros de turmas para serem exibidos.</p>
      {{/if}}
    </main>
  </body>
</html>